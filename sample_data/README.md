# Sample data

This folder is for a couple of pre-run research examples (exported JSON from
a completed `/research/{id}` call) so a reviewer can see real output without
waiting for a live run. Add exports here once a few research runs have been
completed against a live Groq API key.
